# -*- coding: utf-8 -*-
# Temporarily disabled SQL view models due to _unknown object errors
# These models will be re-enabled after fixing the SQL view queries

# The barber service report models were causing _unknown object errors
# due to broken SQL joins or references to non-existent records.
# They need to be refactored with proper error handling.

# Placeholder to prevent import errors
pass